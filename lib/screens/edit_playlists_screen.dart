import 'package:flutter/material.dart';

class EditPlaylistsScreen extends StatelessWidget {
  const EditPlaylistsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
